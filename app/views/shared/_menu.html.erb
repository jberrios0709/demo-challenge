<% def nav_active(locals, name) %>
  <% 'active' if locals[:page] == name %>
<% end %>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Navbar</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-item nav-link <%= nav_active(locals, 'upload_file') %>" href="<%= upload_file_path %>">Upload File</a>
      <a class="nav-item nav-link <%= nav_active(locals, 'history_files') %>" href="<%= files_path %>">History Files</a>
      <a class="nav-item nav-link <%= nav_active(locals, 'contacts') %>" href="<%= contacts_path %>">Contacts</a>
      <a class="nav-item nav-link <%= nav_active(locals, 'errors') %>" href="<%= error_logs_path %>">Errors</a>
      <form class="form-inline" action="/logout" method="post">
        <input type='submit' class="btn btn-sm btn-outline-secondary" value='Log out' url></input>
      </form>
    </div>
  </div>
</nav>